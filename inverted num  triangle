def print_inverted_number_triangle(n):
 
    for i in range(n, 0, -1):
      
        row_str = " ".join(str(j) for j in range(1, i + 1))
        print(row_str)

 
rows = 5
print_inverted_number_triangle(rows)

